- Validates code changes by running the project's own safety net - typecheck, full-repo ESLint, and a production build - before declaring the work finished (the project has no unit-test framework). Confidence: 0.6
- Reports unverified or untestable areas plainly and calls out deployment blockers, rather than claiming full coverage (e.g. warns not to deploy when the staging backend still serves the old contract). Confidence: 0.5
- Keeps throwaway scaffolding (stub servers, ad-hoc test scripts) out of the repo, writing it to the tool's scratchpad instead. Confidence: 0.5
- Cleans up after itself: kills background dev servers and stub processes and confirms ports are free before wrapping up. Confidence: 0.5
- Wants features implemented fully and kept in sync with the backend contract; declines offers to temporarily hide, disable, or gracefully degrade functionality that the backend does not yet support ("I want the code finished, in line with the backend"). Confidence: 0.75
- Expects cross-repo verification: when the frontend depends on a backend endpoint, hunt through the backend source (registered routes, service methods, SQL) to confirm whether it exists or has a replacement, rather than assuming or working around it silently. Confidence: 0.75
- Wants missing/blocking backend endpoints recorded as TODOs in the backend repo itself (the owning repo), so the gap is tracked at its source instead of being patched only on the frontend side. Confidence: 0.7
- Requires a specific skill (the `ponytail` skill - "lazy senior dev": shortest working diff, no unrequested abstractions, no new dependencies, delete over add) to be activated and followed for the entire task, and says so explicitly when handing off work. Confidence: 0.85
- Delegates well-scoped, repetitive multi-file migrations to subagents rather than doing it inline, and expects the scope to be pinned down (in-scope vs. out-of-scope files, the backend routes as source of truth) before the subagent starts. Confidence: 0.75
- Declines offers of live end-to-end/browser verification ("tak perlu") - wants the code finished and contract-aligned, with correctness established by reading the backend source rather than by running servers. Confidence: 0.6
- Prefers honest, non-misleading failure copy: when a feature has no backend route, keep its UI but make it fail with a clear "not available yet" message rather than a generic 404 that reads like data loss. Confidence: 0.6
